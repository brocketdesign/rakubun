extends ../base

block content
  .container
    h1.mt-4.mb-4.text-center タグ一覧

    if tags.length > 0
      .row
        each tag in tags
          .col-6.col-md-4.col-lg-3.mb-3
            a.badge.bg-primary.text-decoration-none.text-white.py-2.px-3.d-block.text-center(href=`/templates/tag/${tag._id}`) #{tag._id} (#{tag.count})
    else
      p タグが見つかりません。

    a.btn.btn-secondary.mt-4(href='/templates') 戻る
